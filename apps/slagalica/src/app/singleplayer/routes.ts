import { Routes } from '@angular/router';
import { SingleplayerPageComponent } from './containers/singleplayer-page';

export const routes: Routes = [
  {
    path: '',
    component: SingleplayerPageComponent
  }
];
